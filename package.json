{
  "name": "react-coreplay",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:coreplay": "pnpm --filter coreplay build",
    "dev:coreplay": "pnpm --filter coreplay dev",
    "build:pluginsdk": "pnpm --filter plugin-sdk build",
    "build:plugintemplate": "pnpm --filter plugin-template build",
    "build:plugins": "node plugins/plugin-sdk/scripts/build-plugins.ts",
    "build:all": "pnpm -r build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "esbuild"
    ]
  }
}
